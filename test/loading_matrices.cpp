

#include "catch.hpp"

#include <zee.hpp>

using TIdx = uint32_t;
using TVal = float;

TEST_CASE("load random matrix", "[loading matrices]") {
    REQUIRE(0 == 0);
}

TEST_CASE("load identity matrix", "[loading matrices]") {
    REQUIRE(0 == 0);
}

TEST_CASE("load matrix market", "[loading matrices]") {
    REQUIRE(0 == 0);
}

TEST_CASE("explicitely set elements", "[loading matrices]") {
    REQUIRE(0 == 0);
}
