Partitioners
############

Optionally a :doc:`DSparseMatrix` can have an associated partitioner. When the matrix is initialized, and when the matrix is used, the partitioner will make either an initial partitioning, or refine the current partitioning.
